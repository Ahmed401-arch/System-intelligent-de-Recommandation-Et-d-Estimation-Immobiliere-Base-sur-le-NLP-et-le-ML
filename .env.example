# Copy to .env and adjust as needed. All variables are optional —
# sensible defaults match the previous hardcoded behaviour.

SECRET_KEY=appart-ai-pfe-2025-secret
FLASK_DEBUG=1
PORT=5000
HOST=0.0.0.0

LOG_LEVEL=INFO

# DB_PATH=data/app.db
# HOUSING_CSV=data/housing.csv
# MODEL_PATH=data/price_model.pkl

ML_RETRAIN_ON_START=0
DEFAULT_TOP_N=5
